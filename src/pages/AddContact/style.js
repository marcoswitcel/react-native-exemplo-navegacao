import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        padding: 15,
    },
    label: {

    },
    input: {
        borderColor: 'black',
        borderWidth: 1,
    },
});