import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    listContainer: {
        paddingLeft: 15,
    },
    listItem: {
        marginVertical: 10,
    },
})

export default style;